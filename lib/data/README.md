# Data Layer

This directory contains data models, repositories, and data sources.

## Structure

- **models/** - Data models with JSON serialization (User, Service, Booking, etc.)
- **repositories/** - Repository implementations for data access
- **datasources/** - Data source implementations
  - **local/** - Local storage (Hive, SharedPreferences)
  - **remote/** - Remote API clients and endpoints
