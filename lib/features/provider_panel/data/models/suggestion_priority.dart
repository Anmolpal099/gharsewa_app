/// Priority level of a suggestion
enum SuggestionPriority {
  /// High priority suggestion with significant impact
  high,
  
  /// Medium priority suggestion
  medium,
  
  /// Low priority suggestion
  low,
}
